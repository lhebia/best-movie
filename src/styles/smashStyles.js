import styled from '@emotion/styled';

export const SmashContainer = styled.div`
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    grid-gap: 1rem;
    margin-bottom: 5rem;
`;

export const SmashTile = styled.div`
    padding: 1rem;
    border: 1px solid white;
    border-radius: 5px;
`;